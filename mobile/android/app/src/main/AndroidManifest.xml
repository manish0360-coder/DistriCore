<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- **Not merged in from debug/profile — each build variant's manifest is independent.**
         `src/debug/AndroidManifest.xml` and `src/profile/AndroidManifest.xml` each declare
         this permission with a comment saying it is "required for development... the
         Flutter tool needs it to communicate with the running application, to allow
         setting breakpoints, to provide hot reload, etc." That comment described why the
         *tool* needs it, not why the *app* does, and this manifest — the one every release
         build actually ships — was left with no declaration at all. A release build is
         `AppConfig`'s entire reason to exist (`lib/app/config.dart`: a compiled-in server
         address), and every `Dio` request it makes would have been refused by the OS
         before leaving the device.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:label="districore"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
